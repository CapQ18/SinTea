{
  "name": "sintea-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sintea_db --file=src/db/schema.sql",
    "db:seed": "wrangler d1 execute sintea_db --file=src/db/seed.sql"
  },
  "dependencies": {
    "hono": "^4.5.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260529.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.6",
    "typescript": "^5.5.3",
    "wrangler": "^3.57.0"
  }
}